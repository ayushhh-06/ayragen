'use client';

import { useEffect, useRef } from 'react';

export const AmbientAudio = () => {
  // Non-rendering ambient audio manager — no UI
  // Placeholder for future ambient audio integration
  return null;
};
